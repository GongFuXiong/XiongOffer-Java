problem8，利用二分查找法实现递增数组寻找最小值
problem11，利用递归计算base的exponent次方:递归到最底层，然后从最底层一层层返回到最高层，计算结果
problem12，利用递归全排列法按顺序打印出从1到最大的n位十进制数；用字符串来表示数字上模拟加法
for循环作用为删除001前面的数字0；代码中必须包含return语句

problem16，输入一个链表，反转链表；定义的两个指针转换难理解，反转即为改变链表的next值

problem19使用循环方式交换每个节点的左右子树位置。利用栈结构，有空指针错误
problem20输入一个矩阵，按照从外向里以顺时针的顺序依次打印出每一个数字..注意前提条件判断语句
problem24输入一个整数数组，判断该数组是不是某二叉搜索树的后序遍历的结果。。。递归操作理解难度大
problem25输入一颗二叉树和一个整数，打印出二叉树中结点值的和为输入整数的所有路径。。。两个函数，过程复杂难理解
problem26输入一个复杂链表,返回结果为复制后复杂链表的head。。。。。完全被赋值搞晕
problem27将二叉搜索树转换为一个排序的双向链表。。。难理解怎么连接根结点和左结点的最大值和右结点的最小值
problem28输入一个字符串，打印出该字符串中字符的所有排列。。。利用循环递归
problem30 输入n个整数，找出其中最小的K个数。。。基于partition函数的O(n)解法，，partition函数的定义很重要，难理解
problem36输入一个数组,求出这个数组中的逆序对的总数P。。。。采用辅助数组和归并排序方法，，难理解
problem43把n个骰子仍在地上，所有骰子朝上一面的点数之和为s，输入n，打印出s的所有可能的值出现的概率，，，两个数组难理解
堆排序过程难理解